@page "/lockuser"
@using RxForEyeIPD.Components.Pages.DynamicFileCreator.Users.CsScript
@using Syncfusion.Blazor.Buttons.Internal
@using Syncfusion.Blazor.Charts.Internal
@using static RxForEyeIPD.Components.Pages.Settings.LocationMaster.Users.srvUsers
@inject IUserDTO userDTO
@inject AuthenticationStateProvider AuthStateProvider
@inject IHttpContextAccessor HttpContextAccessor
@inject IJSRuntime JS

@rendermode InteractiveServer

<PageTitle>Lock User Account</PageTitle>

<div class="row">
    @if (!string.IsNullOrEmpty(ErrorMessage))
    {
    <div class="@alertClass animate__fadeIn" role="alert">
        @ErrorMessage
    </div>
    }
</div>
<div class="card shadow-sm border-0 mb-4">
    <div class="card-header bg-white border-bottom-0 pt-3">
        <h5 class="text-secondary mb-0"><i class="oi oi-lock-locked me-2"></i>Security: Lock User Account</h5>
    </div>
    <div class="card-body">
        <div class="row align-items-center g-3">

            <div class="col-md-5">
                <label class="form-label fw-bold text-muted small mb-1">Lock Device Upto Date & Time</label>
                <div class="input-group">
                    <span class="input-group-text bg-light">
                        <SfDateTimePicker TValue="DateTime ?"
                                          @bind-Value="userEntity.LockUpTo"
                                          Format="dd/MM/yyyy hh:mm tt"
                                          Placeholder="Select date and time">
                        </SfDateTimePicker>
                    </span>
                </div>
            </div>

            <div class="col-md-4 d-flex align-items-center pt-4">
                <div class="d-flex align-items-center p-2 border rounded bg-light w-100">
                    <SfSwitch @ref="DateTImeLockSwitch"
                              TChecked="bool"
                              @bind-Checked="isDateTImeLocked"
                              ValueChange="OnDateSwitchChange"
                              OnLabel="LOCKED"
                              OffLabel="ACTIVE"
                              CssClass="wide-switch">
                    </SfSwitch>
                    <span class="ms-3 small fw-bold @(isDateTImeLocked ? "text-danger" : "text-success")">
                        @(isDateTImeLocked ? "Account is Restricted" : "Account is Active")
                    </span>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="card shadow-sm border-0 mb-4">
    <div class="card-header bg-white border-bottom-0 pt-3">
        <h5 class="text-secondary mb-0"><i class="oi oi-lock-locked me-2"></i>Label Info</h5>
    </div>
    <div class="card-body">
        <div class="row align-items-center g-3">

            <div class="col-md-5">
                <label class="form-label fw-bold text-muted small mb-1">This Device Ip Address</label>
                <div class="input-group">
                    <span class="input-group-text bg-light">
                        <SfTextBox TValue="string?" @bind-Value="ipAddress" />
                    </span>
                </div>
            </div>

            <div class="col-md-4 d-flex align-items-center pt-4">
                <div class="d-flex align-items-center p-2 border rounded bg-light w-100">
                    <SfSwitch TChecked="bool"
                              @ref="IpAddressLockSwitch"
                              @bind-Checked="isIpAddressLocked"
                              OnLabel="Set this ip address as my fixed account"
                              OffLabel="Remove this ip address as my fixed account"
                              ValueChange="OnIpAddressSwitchChange"
                              CssClass="wide-switch">
                    </SfSwitch>
                    <span class="ms-3 small fw-bold @(isIpAddressLocked ? "text-danger" : "text-success")">
                        @(isDateTImeLocked ? "User Account Locked By Ip Address Removed" : "User Account Locked By Ip Address")
                    </span>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="card shadow-sm border-0 mb-4">
    <div class="card-header bg-white border-bottom-0 pt-3">
        <h5 class="text-secondary mb-0"><i class="oi oi-lock-locked me-2"></i>Label Info</h5>
    </div>
    <div class="card-body">
        <div class="row align-items-center g-3">

            <div class="col-md-5">
                <label class="form-label fw-bold text-muted small mb-1">Lock User Account</label>
                <div class="input-group">
                    <span class="input-group-text bg-light">
                        Calander
                    </span>
                </div>
            </div>

            <div class="col-md-4 d-flex align-items-center pt-4">
                <div class="d-flex align-items-center p-2 border rounded bg-light w-100">
                    <SfSwitch TChecked="bool"
                              @ref="DeviceInfoLockSwitch"
                              @bind-Checked="isDeviceInfoLocked"
                              OnLabel="Active this device as my primary device"
                              OffLabel="Remove this device as my primary device"
                              ValueChange="OnDeviceInfoSwitchChange"
                              CssClass="wide-switch">
                    </SfSwitch>
                    <span class="ms-3 small fw-bold @(isDeviceInfoLocked ? "text-danger" : "text-success")">
                        @(isDateTImeLocked ? "User Account Locked By Device Info Removed" : "User Account Locked By Device Info")
                    </span>
                </div>
            </div>
        </div>
    </div>
</div>
<style>
    /* 1. Increase the overall width of the switch container */
    .wide-switch.e-switch-wrapper {
        width: 320px; /* Adjust this value until the full text fits */
        height: 30px;
    }

        /* 2. Adjust the 'On' text position */
        .wide-switch.e-switch-wrapper .e-switch-on {
            text-indent: -40px; /* Moves text so it doesn't hide under the handle */
            font-size: 12px;
        }

        /* 3. Adjust the 'Off' text position */
        .wide-switch.e-switch-wrapper .e-switch-off {
            text-indent: 40px;
            font-size: 12px;
        }

        /* 4. Ensure the handle slides far enough to the right */
        .wide-switch.e-switch-wrapper.e-switch-active .e-switch-handle {
            left: 300px; /* (Width of wrapper - Width of handle) */
        }
</style>

<style>
    /* Make the switch slightly larger and easier to hit */
    .custom-switch-size.e-switch-wrapper {
        width: 100px;
        height: 32px;
    }

    /* Ensure the DateTimePicker looks modern */
    .e-datetimepicker.e-control-wrapper.e-outline {
        border-radius: 8px;
    }

    /* Smooth transition for the alert */
    .animate__fadeIn {
        animation: fadeIn 0.5s;
    }

    @@keyframes fadeIn {
        from {
            opacity: 0;
            transform: translateY(-10px);
        }

        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    /* Align the switch handle correctly for the wider width */
    .custom-switch-size.e-switch-wrapper.e-switch-active .e-switch-handle {
        left: 68px;
    }

</style>
@code {
    private bool isDateTImeLocked; // Track the switch state
    private SfSwitch<bool> DateTImeLockSwitch; // Reference to the switch component

    private bool isDeviceInfoLocked; // Track the switch state
    private SfSwitch<bool> DeviceInfoLockSwitch;

    private bool isIpAddressLocked; // Track the switch state
    private SfSwitch<bool> IpAddressLockSwitch;

    string alertClass = "alert alert-danger";
    private string ErrorMessage { get; set; } = string.Empty;
    private string ipAddress { get; set; } = string.Empty;
    private string? ip { get; set; } = string.Empty;
    private UsersEntity userEntity = new();
    private UserAccessStatus? userAccessStatus = new();
    int userId;
    private string? deviceData;

    protected override async Task OnInitializedAsync()
    {
        isDateTImeLocked = false;
        isDeviceInfoLocked = false;
        isIpAddressLocked = false;

        var authState = await AuthStateProvider.GetAuthenticationStateAsync();
        userId = authState.User.GetUserId() ?? 0;
        userAccessStatus = await userDTO.GetUserAccessStatus(userId);

        if (userAccessStatus != null)
        {
            if (!string.IsNullOrEmpty(userAccessStatus.LockUpTo))
            {
                isDateTImeLocked = true;
            }
            if (!string.IsNullOrEmpty(userAccessStatus.DeviceId))
            {
                isDeviceInfoLocked = true;
            }
            if (!string.IsNullOrEmpty(userAccessStatus.IpAddress))
            {
                isIpAddressLocked = true;
                ipAddress = userAccessStatus.IpAddress;
            }
            else
            {
                ip = HttpContextAccessor.HttpContext?.Connection.RemoteIpAddress?.ToString();
                if (string.IsNullOrEmpty(ip))
                {
                    ipAddress = "Unable to retrieve IP address.";
                }
                else
                {
                    ipAddress = ip ?? "";
                }
            }
        }
        StateHasChanged();
    }

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            try
            {


                deviceData = userId.ToString() + await JS.InvokeAsync<string>("getDeviceInfo");


                StateHasChanged();
            }
            catch (Exception ex)
            {
                Console.WriteLine($"Error: {ex.Message}");
            }
        }
    }



    private async Task OnDateSwitchChange(Syncfusion.Blazor.Buttons.ChangeEventArgs<bool> args)
    {
        ErrorMessage = string.Empty;

        // VALIDATION: Check if the date is selected and in the future
        if (userEntity.LockUpTo == null || userEntity.LockUpTo <= DateTime.Now)
        {
            ErrorMessage = "Select a Date or Time Greater Than Current Date or Time";

            // CRITICAL: Force the switch back to OFF
            isDateTImeLocked = false;

            // Tell Blazor to re-render the UI immediately to show the switch moved back
            StateHasChanged();
            return;
        }

        var authState = await AuthStateProvider.GetAuthenticationStateAsync();
        var user = authState.User;
        userEntity.UserId = user.GetUserId() ?? 0;

        if (args.Checked)
        {
            int IsSuccess = await userDTO.UpdateLockUpTo(userEntity);
            if (IsSuccess > 0)
            {
                ErrorMessage = "Account locked until " + userEntity.LockUpTo.ToString();
                alertClass = "alert alert-success";

                await Task.Delay(2000);
                ErrorMessage = string.Empty;
                StateHasChanged();
            }
            else
            {
                isDateTImeLocked = false; // Reset if DB update fails
                ErrorMessage = "Failed to lock the account.";
            }
        }
        else
        {
            // Unlock logic
            await userDTO.UpdateLockUpTo(new UsersEntity
            {
                UserId = userEntity.UserId,
                LockUpTo = null
            });
            ErrorMessage = "Account unlocked.";
            alertClass = "alert alert-info";
        }
    }

    private async Task OnIpAddressSwitchChange(Syncfusion.Blazor.Buttons.ChangeEventArgs<bool> args)
    {
        ErrorMessage = string.Empty;
        var authState = await AuthStateProvider.GetAuthenticationStateAsync();
        var user = authState.User;
        if (user.Identity is not null && user.Identity.IsAuthenticated)
        {
            // Extract the ID from claims (Ensure GetUserId() extension is working)
            userEntity.UserId = user.GetUserId() ?? 0;
            userEntity.UpdatedBy = userEntity.UserId;
        }
        if (userEntity.UserId == 0)
        {
            isIpAddressLocked = false;
            ErrorMessage = "User session not found. Please re-login.";
            return;
        }
        if (string.IsNullOrEmpty(ip))
        {
            isIpAddressLocked = false;
            ErrorMessage = "IP address cannot be empty.";
            return;
        }

        if (args.Checked)
        {
            try
            {
                await userDTO.DeviceIpAddress(ip, userEntity.UserId);
                ErrorMessage = "This Device has been locked up as your primary device with IP: " + ipAddress;
                alertClass = "alert alert-success";
            }
            catch (Exception ex)
            {
                ErrorMessage = "JS Error: " + ex.Message;
            }
        }
        else
        {
            await userDTO.DeviceIpAddress("NULL", userEntity.UserId);
            ErrorMessage = "This Device IP Address has been removed as your primary device.";
            alertClass = "alert alert-danger";
        }

    }

    private async Task OnDeviceInfoSwitchChange(Syncfusion.Blazor.Buttons.ChangeEventArgs<bool> args)
    {
        ErrorMessage = string.Empty;

        // 1. Get User Identity first
        var authState = await AuthStateProvider.GetAuthenticationStateAsync();
        var user = authState.User;
        int currentUserId = user.GetUserId() ?? 0;

        if (currentUserId == 0)
        {
            isDeviceInfoLocked = false;
            ErrorMessage = "User session not found. Please re-login.";
            return;
        }

        // 2. Handle the "Locking" (Switch turned ON)
        if (args.Checked)
        {
            try
            {
                // Call JS to get the device string
                var deviceInfoRaw = await JS.InvokeAsync<string>("getDeviceInfo");

                if (string.IsNullOrEmpty(deviceInfoRaw))
                {
                    isDeviceInfoLocked = false;
                    ErrorMessage = "Device information could not be retrieved.";
                    return;
                }

                // Combine ID and Device Info
                deviceData = $"{currentUserId}{deviceInfoRaw}";

                // Save to Database
                await userDTO.DeviceInfo(deviceData, currentUserId);

                ErrorMessage = "This device is now locked as your Primary Device.";
                alertClass = "alert alert-success";
            }
            catch (Exception ex)
            {
                ErrorMessage = "JS Error: " + ex.Message;
            }
        }
        // 3. Handle the "Unlocking" (Switch turned OFF)
        else
        {
            deviceData = "NULL";
            await userDTO.DeviceInfo(deviceData, currentUserId);

            ErrorMessage = "This device has been removed as your primary device.";
            alertClass = "alert alert-danger";
        }

        StateHasChanged();
    }
}
